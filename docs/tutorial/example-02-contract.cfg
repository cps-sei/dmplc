system = main
initially = "loc()==hover & real_x==0 & goal_x==0 & DONE==0"

output-variables = "real_x, goal_x"
forbidden = "loc()==error"
scenario = "stc"
directions = "oct"
sampling-time = 0.1
time-horizon = 20
iter-max = 100
output-format = "GEN"

